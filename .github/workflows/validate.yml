name: Validate

on:
  push:
  pull_request:

jobs:
  addon-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Lint the add-on
        uses: frenck/action-addon-linter@v2
        with:
          path: "./urban_arrow_battery"
